# RAG 基础与边界

## 定义

RAG 的中文名称是检索增强生成。它先从受控知识库检索相关片段，再把片段作为上下文交给生成模型回答。

## 标准链路

标准链路包含四步：文档分块、Embedding 向量化、相似片段检索、基于上下文生成答案。检索结果必须在送入模型前校验知识空间和文档当前版本。

## 主要收益

RAG 的主要收益是让答案能够引用可核验资料，并能通过更新知识库获得新信息。它可以降低模型凭空编造的概率，但不能保证绝对没有错误。

## 回答边界

资料没有支持信息时，系统应明确回答“根据当前知识空间的资料，没有找到可支持该问题的信息”，不能改用模型自身知识猜测。
